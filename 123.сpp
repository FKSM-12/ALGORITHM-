include <iostream>
include <vector>
using namespace std;

int main() {
    // создаем вектор, который будем использовать как стек
    vector <char> stack;

    stack.push_back('q'); // записываем 'q' в стек
    stack.push_back('l'); // записываем 'l' в стек
    stack.push_back('w'); // записываем 'w' в стек

    // выводим все элементы стека
    cout << "Элементы стека: ";
    for(char i : stack) {
        cout << i << " ";
    }
    cout << endl;

    return 0;
}
